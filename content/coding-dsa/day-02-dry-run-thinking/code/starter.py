def execute_and_verify(code_snippet, expected):
    """Trace through a code snippet and verify its output matches expectation.

    This function is a wrapper for mental practice. You will:
    1. Read the code_snippet as a string
    2. Trace through it line by line (mentally, on paper)
    3. Record the final value
    4. Compare to expected

    For this exercise, trace each snippet manually and return
    whether your trace matches the expected result.

    Args:
        code_snippet: string describing the code (e.g. "x = 3; y = 4; print(x + y)")
        expected: the correct output for the code snippet

    Returns:
        bool: True if the final result matches expected
    """
    # TODO: Trace through the code. Write the logic to determine
    # what the final result would be. Return True if it matches expected.
    return True
