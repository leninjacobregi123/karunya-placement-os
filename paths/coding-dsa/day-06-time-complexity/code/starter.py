def count_iterations(n, m):
    """Count total iterations of: for i in range(n): for j in range(m).

    Args:
        n: outer loop size
        m: inner loop size

    Returns:
        int: total inner iterations
"""
    # Complete this function
    pass
